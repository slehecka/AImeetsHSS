---
import LocaleLink, { type Props as LocaleLinkProps } from "@/components/locale-link.astro";

interface Props extends Omit<LocaleLinkProps, "class"> {}

const props = Astro.props;
---

<LocaleLink
	{...props}
	class="font-emphasised transition hover:text-text-brand aria-[current]:font-strong"
>
	<slot />
</LocaleLink>
